import random

random.randint(1,100)
#generates random no from 1 to 100

random.randrange(1,10,2)
#generates a random no with step size 2 from 1 to 10 excluding 10

random.random()
#generates a random float no between 0 and 1

random.choice([list])
#returns an element from list

''.join(random.sample(word,len(word)))
